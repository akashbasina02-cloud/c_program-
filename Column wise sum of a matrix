#include <stdio.h>

int main() {
    int n, m;
    scanf("%d %d", &n, &m);   // Read rows and columns

    int arr[n][m];

    // Read matrix elements
    for(int i = 0; i < n; i++) {
        for(int j = 0; j < m; j++) {
            scanf("%d", &arr[i][j]);
        }
    }

    // Calculate column-wise sum
    for(int j = 0; j < m; j++) {
        int sum = 0;   // Reset sum for each column

        for(int i = 0; i < n; i++) {
            sum += arr[i][j];   // Add column elements
        }

        printf("%d ", sum);   // Print column sum
    }

    return 0;
}
