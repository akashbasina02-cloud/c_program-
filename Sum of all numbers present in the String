#include <stdio.h>

int main() {
    char str[10000];
    int sum = 0;

    scanf("%s", str);

    for(int i = 0; str[i] != '\0'; i++) {
        if(str[i] >= '0' && str[i] <= '9') {
            sum += str[i] - '0';
        }
    }

    printf("%d", sum);

    return 0;
}
