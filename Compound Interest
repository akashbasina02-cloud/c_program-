#include <stdio.h>
#include <math.h>

int main() {
    int P, R, T;
    scanf("%d %d %d", &P, &R, &T);

    double amount = P * pow((1 + R / 100.0), T);
    double ci = amount - P;

    printf("%.2f", ci);
    return 0;
}
