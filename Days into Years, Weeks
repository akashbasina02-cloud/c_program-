#include <stdio.h>

int main() {
    int D;
    scanf("%d", &D);

    int years = D / 365;
    int weeks = (D % 365) / 7;

    printf("%d years", years);
    printf("%d weeks ", weeks);

    return 0;
}
