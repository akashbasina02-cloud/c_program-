#include <stdio.h>

int main() {
    int n, i;
    scanf("%d", &n);

    int mid = (n + 1) / 2;

    for (i = n; i >= 1; i--) {
        if (i == mid) {
            printf("%d\n", i);
        } else {
            printf("%d %d\n", i, i);
        }
        printf("\n");
    }

    return 0;
}
