#include <stdio.h>

int main() {
    int n, m;
    
    // Read rows and columns
    scanf("%d %d", &n, &m);

    int a[n][m], b[n][m];

    // Read first matrix
    for(int i = 0; i < n; i++) {
        for(int j = 0; j < m; j++) {
            scanf("%d", &a[i][j]);
        }
    }

    // Read rows and columns again
    scanf("%d %d", &n, &m);

    // Read second matrix
    for(int i = 0; i < n; i++) {
        for(int j = 0; j < m; j++) {
            scanf("%d", &b[i][j]);
        }
    }

    // Print resultant matrix
    for(int i = 0; i < n; i++) {
        for(int j = 0; j < m; j++) {
            printf("%d ", a[i][j] + b[i][j]);
        }
        printf("\n");
    }

    return 0;
}
