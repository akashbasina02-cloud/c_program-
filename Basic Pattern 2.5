#include <stdio.h>

int main() {
    int N, i, j;
    scanf("%d", &N);

    for(i = N; i >= 1; i--) {
        char ch = 'A' + i - 1;
        for(j = i; j <= N; j++) {
            printf("%c ", ch);
        }
        printf("\n");
    }

    return 0;
}
