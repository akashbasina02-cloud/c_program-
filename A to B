#include <stdio.h>

int main() {
    int A, B;   // declare variables

    scanf("%d %d", &A, &B);   // read A and B

    // loop from A to B
    for (int i = A; i <= B; i++) {
        printf("%d ", i);   // print number with space
    }

    return 0;
}
