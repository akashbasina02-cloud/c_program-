#include <stdio.h>

int main()
{
    int N, i;          // Declare variables

    scanf("%d", &N);   // Read the value of N

    // Loop starts from 1 (first odd number)
    // Increment by 2 to get only odd numbers
    for (i = 1; i <= N; i += 2)
    {
        printf("%d ", i);   // Print odd number
    }

    return 0;          // End of program
}
