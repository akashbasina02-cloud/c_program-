#include <stdio.h>
#include <string.h>

int main() {
    char A[45];

    scanf("%s", A);

    int n = strlen(A);

    if (A[0] == 's' || A[n - 1] == 's')
        printf("Yes\n");
    else
        printf("No\n");

    return 0;
}
