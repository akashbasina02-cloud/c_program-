#include <stdio.h>

int main() {
    int X;

    // Read Chef's seat number
    scanf("%d", &X);

    // If X is the last seat of a row,
    // X+1 will be in the next row
    if (X % 5 == 0) {
        printf("No\n");
    } else {
        printf("Yes\n");
    }

    return 0;
}
