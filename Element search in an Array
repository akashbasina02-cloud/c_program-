#include<stdio.h>

int main() {
    int n, i, key;
    int found = 0;

    scanf("%d", &n);   // Read size of array

    int arr[n];

    for(i = 0; i < n; i++) {
        scanf("%d", &arr[i]);   // Read array elements
    }

    scanf("%d", &key);   // Read element to search

    for(i = 0; i < n; i++) {
        if(arr[i] == key) {
            found = 1;   // Element found
            break;
        }
    }

    if(found == 1)
        printf("True");
    else
        printf("False");

    return 0;
}
