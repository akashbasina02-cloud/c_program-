#include <stdio.h>

int main() {
    int n;
    
    // Read size of array
    scanf("%d", &n);
    
    int arr[n];
    
    // Read array elements
    for(int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }
    
    int A, B;
    
    // Read A and B
    scanf("%d %d", &A, &B);
    
    int sum = 0;
    
    // Calculate sum of elements NOT between A and B
    for(int i = 0; i < n; i++) {
        
        // If element is outside the range [A, B]
        if(arr[i] < A || arr[i] > B) {
            sum += arr[i];
        }
    }
    
    // Print result
    printf("%d", sum);
    
    return 0;
}
