#include <stdio.h>

int main() {
    int n;   // declare variable

    scanf("%d", &n);   // read input number

    // loop from 1 to n
    for (int i = 1; i <= n; i++) {
        if (n % i == 0) {   // check if i is factor
            printf("%d ", i);   // print factor
        }
    }

    return 0;
}
