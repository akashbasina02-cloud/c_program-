#include <stdio.h>

int main() {
    int A, B;
    
    // Read the lower and upper bounds
    scanf("%d %d", &A, &B);

    // Loop through each number from A to B
    for (int num = A; num <= B; num++) {
        int isSelfDividing = 1;   // Assume number is self-dividing

        // Extract digits using for loop
        for (int temp = num; temp > 0; temp /= 10) {
            int digit = temp % 10;   // Get last digit

            // If digit is 0 or number not divisible by digit
            if (digit == 0 || num % digit != 0) {
                isSelfDividing = 0;  // Mark as not self-dividing
                break;               // Stop checking further digits
            }
        }

        // If number is self-dividing, print it
        if (isSelfDividing) {
            printf("%d ", num);
        }
    }

    return 0;
}
